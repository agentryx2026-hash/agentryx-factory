# Phase 11 — Cost and Quota Dashboard

**One-liner**: Per-project, per-agent, per-model spend visualization. Alerts at threshold, hard caps trigger Hermes notifications. Builds on the `llm_calls` table from Phase 2.

*(sketch — expanded when phase becomes active)*
