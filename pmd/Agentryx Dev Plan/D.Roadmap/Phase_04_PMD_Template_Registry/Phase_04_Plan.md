# Phase 4 — PMD Template Registry

**One-liner**: Formalize the 25-30 standard PMD documents as versioned templates with a dependency graph (e.g. Architecture depends on Scope; Test Plan depends on Architecture).

*(sketch — expanded when phase becomes active)*
