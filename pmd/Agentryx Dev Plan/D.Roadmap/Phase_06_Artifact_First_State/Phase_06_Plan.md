# Phase 6 — Artifact-First State

**One-liner**: Replace text-blob state (`codeOutput`, `qaReport`) with typed, versioned artifacts (PMD doc / source tree / test report / training script) in an artifact store. Enables clean diff, replay, hand-off.

*(sketch — expanded when phase becomes active)*
