# Phase 12 — B7 Admin Module v1

**One-liner**: Implement the existing PMD `B7 Admin & Operations Module` standard — Super Admin / Admin / Operator role hierarchy, `config_settings` table, key manager (toggleable without delete), feature flags, masked sensitive values.

See `pmd/Agentryx Dev Plan/B.Agentryx Edge/B7_Admin_Operations_Module.md` for the full spec.

*(sketch — expanded when phase becomes active)*
