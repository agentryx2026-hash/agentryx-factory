# B1: API Reference — {Project Name}
> **Template Version:** 1.0 | **Auto-Generated By:** Crusher (Documentation Agent)
> **Date:** {YYYY-MM-DD} | **Source:** Generated from application source code

---

## Base URL
```
{Production}:  https://{domain}/api/v1
{Staging}:     https://staging.{domain}/api/v1
{Local Dev}:   http://localhost:{port}/api/v1
```

## Authentication
All authenticated endpoints require:
```
Authorization: Bearer {jwt_token}
```

## Common Error Format
```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable message",
    "details": []
  }
}
```

---

## Endpoints

### {Module 1 Name}

#### `POST /api/v1/{resource}`
> {One-line description}

**Auth:** {Required / Public}

**Request Body:**
```json
{
  "field1": "string",
  "field2": 123
}
```

**Success Response (201):**
```json
{
  "success": true,
  "data": { "id": "uuid", "field1": "value" }
}
```

**Error Responses:**
| Status | Code | When |
|--------|------|------|
| 400 | VALIDATION_ERROR | Invalid input |
| 401 | UNAUTHORIZED | Missing/expired token |
| 409 | CONFLICT | Duplicate resource |

**Example (curl):**
```bash
curl -X POST https://{domain}/api/v1/{resource} \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {token}" \
  -d '{"field1": "value", "field2": 123}'
```

**Example (fetch):**
```javascript
const res = await fetch('/api/v1/{resource}', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` },
  body: JSON.stringify({ field1: 'value', field2: 123 })
});
const data = await res.json();
```

---

#### `GET /api/v1/{resource}`
> {One-line description}

**Auth:** {Required / Public}

**Query Parameters:**
| Param | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| page | number | No | 1 | Page number |
| limit | number | No | 20 | Items per page |
| sort | string | No | created_at:desc | Sort field:order |

**Success Response (200):**
```json
{
  "success": true,
  "data": [],
  "pagination": { "page": 1, "limit": 20, "total": 85 }
}
```

---

> *Repeat for every endpoint in the application.
> Crusher auto-generates this from the source code + A5 PRD specs.*

---

## Rate Limits
| Endpoint | Limit | Window |
|----------|-------|--------|
| All authenticated | {100 requests} | {per minute} |
| Auth endpoints | {10 requests} | {per minute} |
| Public endpoints | {50 requests} | {per minute} |

## Versioning
| Version | Status | Base URL |
|---------|--------|----------|
| v1 | Active | `/api/v1/` |
